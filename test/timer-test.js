var expect = require('chai').expect;

describe('timer controls', function() {

  describe( 'add button' function() {
    it('should create a new timer with same original time as last timer if timers exist', function() {
        // expect(timers).to.deep.equal();
    });

    it('should create a new timer with an incremented name as last timer if timers exist', function() {
        // expect(timers).to.deep.equal();
    });

    it('should create a 1 minute timer named "Timer 1" if no timers exist', function() {
        // expect(timers).to.deep.equal();
    });
  });

  describe( 'delete button' function() {
    it('should delete the selected button', function() {
        // expect(timers).to.deep.equal();
    });
  });


});